# Sharing playground using websockets
## Install dependencies

```bash
cd server
npm install
cd ../app
npm install
npm start
```

Open each example in multiple browsers/tabs and all of the instances will share their io with other instances.
